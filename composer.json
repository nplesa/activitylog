{
    "name": "nplesa/activitylog",
    "description": "Laravel 12 package to log all model operations and HTTP requests.",
    "type": "library",
    "license": "MIT",
    "required": {
        "php": "^8.2",
        "illuminate/support": "^12.0"
    },
    "autoload": {
        "psr-4": {
            "nplesa\\ActivityLog\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "nplesa\\ActivityLog\\ActivityLogServiceProvider"
            ]
        }
    }
}
